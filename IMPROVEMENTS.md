# 改进建议与已知局限

## 版本变更记录

### v1.6.0（2026-07-20）

| 变更项 | 类型 | 说明 |
|--------|------|------|
| 诊断报告保存 | ✨ 功能 | Ctrl+Shift+S 同时保存 MHTML + `_debug.json`，含所有 JS 源码、事件监听清单、storage、全局对象、环境信息 |
| document_start 注入 | ⚙️ 架构 | 新建 `utils/debugCapture.js`，在页面脚本前劫持 `addEventListener`，记录所有事件监听和 DOM0 事件 |
| 外部 JS 源码收集 | ✨ 功能 | 自动 fetch 所有 `<script src>` 获取完整 JS 源码，支持离线反作弊代码审查 |

### v1.5.3（2026-07-20）

| 变更项 | 类型 | 说明 |
|--------|------|------|
| 保存格式升级为 MHTML | ✨ 优化 | 用 `chrome.pageCapture.saveAsMHTML()` 一步捕获完整页面，CSS/JS/图片全部内嵌，单文件打开即完整布局 |
| 简化保存管线 | ♻️ 重构 | 去掉 content→background 往返，background 直接调用 pageCapture API |
| pageCapture 权限 | ⚙️ 配置 | manifest 新增 `pageCapture` 权限 |

### v1.5.2（2026-07-20）

| 变更项 | 类型 | 说明 |
|--------|------|------|
| 保存路径改为桌面 | ✨ 优化 | `../Desktop/` 相对路径，换电脑自动适配 |
| 更新说明文档 | 📝 文档 | README 保存位置描述更新 |

### v1.5.1（2026-07-20）

| 变更项 | 类型 | 说明 |
|--------|------|------|
| 后台保存页面 | ✨ 功能 | Ctrl+Shift+S 静默保存完整页面 HTML，不弹对话框、不触发切屏检测 |
| FloatPanel Toast 通知 | ✨ 功能 | `showToast()` 方法，保存成功/失败时在页面中央显示3秒通知 |
| downloads 权限 | ⚙️ 配置 | manifest 新增 `downloads` 权限，使用 `chrome.downloads.download({saveAs:false})` 静默下载 |

### v1.5.0（2026-07-17）

| 变更项 | 类型 | 说明 |
|--------|------|------|
| DOM 签名随机化 | 🔒 安全加固 | 所有注入元素 ID/class 用 `crypto.randomUUID()` 生成随机前缀，消除固定 `__leh_` 签名 |
| CSS 动态注入 | 🔒 安全加固 | 样式不再走静态 `content.css`，改为运行时 `_injectCSS()` 动态创建 `<style>` 标签 |
| 风险评估更新 | 📝 文档 | 风险表新增 DOM 签名检测、isTrusted 检测、行为分析三项，概率均 <1% |

### v1.4.0 及之前

| 变更项 | 类型 | 说明 |
|--------|------|------|
| Element UI 兼容 | 🐛 修复 | 支持国网考试系统常用的 Element UI 表单组件 |
| 悬停逐题作答 | ✨ 功能 | 鼠标悬停题目时单独匹配并显示结果 |
| 题库导入/导出 | ✨ 功能 | 支持 Excel/JSON 双格式导入导出 |
| 隐形模式 | ✨ 功能 | Ctrl+Shift+H 纯后台答题，零界面 |
| 中途启动纠错 | ✨ 功能 | 考试做到一半再开，已做对的跳过，做错的纠正 |
| Popup 安全改造 | 🔒 安全 | popup 改为纯状态展示，防止误触失焦 |
| 扩展隐身 | 🔒 安全 | manifest 随机名 + 动态注入 + 无全局变量污染 |

## 已采纳的改进（v1 包含）

| 改进点 | 原PRD | 修正后 |
|--------|-------|--------|
| 扩展隐身 | 无 | manifest随机名 + 动态注入 + 无全局变量污染 |
| 题库存储位置 | content script IndexedDB | background IndexedDB，跨域名通用 |
| 模式分层 | 单一自动模式 | 普通模式 + 手动辅助模式 |
| 题库预置 | 仅导入 | 支持打包预置 + 在线导入双轨 |

## v2 计划迭代项

| 功能 | 优先级 | 说明 |
|------|:---:|------|
| 弃用 dispatchEvent | 🟡 | 只靠原生 click() + checked 属性操作 DOM，消除 isTrusted=false 指纹 |
| 答题节奏扰动 | 🟡 | Stealth 模式偶尔跳过一两题回头补答，模拟人类犹豫行为 |
| Canvas截图OCR兜底 | 🔴 | 考试系统防作弊升级时的生存保障 |
| 自然模式（模拟鼠标轨迹+思考停顿） | 🔴 | 应对行为分析检测 |
| 答题进度统计 | 🟡 | 悬浮窗显示已答/总数/成功率 |
| 题库分组管理 | 🟡 | 按考试类型分组，一键切换 |
| 快捷键自定义 | 🟢 | 允许用户修改快捷键 |
| 错题标记与反馈 | 🟢 | 手动标记错误匹配，优化后续 |

## 已知局限（需用户认知）

1. **非表单类考试不支持**：Canvas渲染、Flash/WebGL渲染、纯图片题目无法处理
2. **跨域iframe受限**：部分考试系统的iframe跨域时content script虽能注入但DOM访问受同源策略限制
3. **移动端不支持**：Chrome Android不完全支持扩展，仅桌面端可用
4. **题库需自行整理**：不提供预置题库，用户需自行导入
5. **纯文本匹配**：不支持图片题、音频题、视频题
